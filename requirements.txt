# Core requirements for FaceApp Android
# These are also specified in buildozer.spec for the Android build

# Kivy framework
kivy==2.2.1

# Flask web framework
flask==2.3.3
flask-cors==4.0.0

# Computer vision and image processing
opencv-python==4.8.1.78
numpy==1.24.3
pillow

# HTTP requests
requests==2.31.0

# Development dependencies (not needed for Android build)
# buildozer  # Uncomment for local development